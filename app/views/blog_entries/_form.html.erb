<%= form_for @entry do |f| %>

	<% @entry.errors.full_messages.each do |message| %>
		<div class="alert alert-warning"	>
			<%= message %>
		</div>
	<% end %>

	<%= f.text_field :title %> </br>
	<%= f.text_area :body %> </br>
	<%= f.submit %>

<% end %>