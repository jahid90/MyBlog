.post-wrapper
  - if not @post
    .well

      %div No such post found. Are you sure this post exists and you have the authority to view this?

  - else
    .well

      %h2= @post.title

      %div= @post.body

    = render 'post_actions'

    %hr/

    .well
      - if @post.comments.any?
        %h3
          What others have to say about this post
        - @post.comments.each do |comment|
          .comment
            %p.author
              = comment.author
              says:
            - if @is_owner
              %div.pull-right
                = link_to [@post, comment], confirm: 'Are you sure?', method: :delete do
                  %i.glyphicon.glyphicon-trash
            %p.content
              = comment.content
          %hr/
      - else
        %p There are no comments on this post yet

    %hr/

    .well
      %p Add a comment
      = form_for [@post, @post.comments.build] do |f|
        .form-group
          = f.label :name
          %br/
          = f.text_field :author

        .form-group
          = f.label :message
          %br/
          = f.text_area :content

        .form-group
          = f.submit